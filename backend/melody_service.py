from ai.generate import generate_melody_from_parameters

def generate_simple_melody(tempo=120, tone="C", emotion="happy"):
    # Llamar a la función que genera melodías usando parámetros
    return generate_melody_from_parameters(tempo, tone, emotion)
